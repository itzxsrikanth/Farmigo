# Additional dependencies required only for running the standalone Streamlit dashboard (streamlit_app.py)
# Run with: pip install -r requirements-streamlit.txt && streamlit run streamlit_app.py
streamlit>=1.30.0
pandas>=2.0.0
